'use client';

/**
 * @deprecated This component is no longer used after migrating to EVM/Mantle.
 * Wagmi handles auto-connect automatically via configuration.
 * Keeping as a stub for backwards compatibility.
 */

export function WalletAutoConnect() {
  // Wagmi/RainbowKit handles auto-connect via configuration
  return null;
}
